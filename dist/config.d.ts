/// <reference types="screeps" />
export declare const config: {
    DEFAULT_MOVE_OPTS: {
        serializeMemory: boolean;
        visualizePathStyle: PolyStyle;
    };
    MEMORY_CACHE_PATH: string;
};
