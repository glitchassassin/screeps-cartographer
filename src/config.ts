export const config = {
  DEFAULT_MOVE_OPTS: {
    serializeMemory: false,
    visualizePathStyle: {
      fill: 'transparent',
      stroke: '#fff',
      lineStyle: 'dashed',
      strokeWidth: 0.15,
      opacity: 0.1
    } as PolyStyle
  },
  MEMORY_CACHE_PATH: '_cg'
};
